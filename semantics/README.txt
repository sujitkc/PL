const: A language with only constant expressions.
if: A language with if expressions, which are raw boolean.
if_bool: A language with boolean expressions.
let: A language with identifiers and let bindings.
let2: A language with identifiers and only one form of expressions.
proc: A language with procedures.
typecheck: A language with static types and type checking.
